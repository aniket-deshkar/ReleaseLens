# Demo scenarios

The `sample-apps/commerce-platform` tree provides controller, service, Kafka, and Flyway examples. Create two Git revisions around a change to demonstrate API DTO changes, security annotation removal, destructive migrations, event contract changes, configuration changes, and safe internal refactoring. The UI shows each finding with its exact evidence.
