# Decision engine

Jev is called once per analysis with typed Choice, Score, and Noul questions. Java owns thresholds and outcome precedence. Critical deterministic findings block release; Jev probabilities can require review but cannot downgrade deterministic severity. Luna receives evidence, paths, decisions, and policy only and returns a validated engineering summary.
