# Evaluation

Evaluation fixtures should use committed base/head pairs and assert finding type, evidence path and lines, graph relationships, blast-radius paths, policy threshold boundaries, and offline behavior. Any accuracy or coverage number must be calculated from those fixtures; the project does not publish unmeasured metrics.
